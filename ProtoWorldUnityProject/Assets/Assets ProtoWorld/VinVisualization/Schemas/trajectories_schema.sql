CREATE TABLE trajectory_paths (
    "tid" INTEGER NOT NULL,
    "vid" INTEGER,
    "seq" INTEGER NOT NULL,
    "nodeid" INTEGER,
    "edgeid" INTEGER,
    "cost" REAL,
    "id" INTEGER,
    "osm_id" INTEGER
);
